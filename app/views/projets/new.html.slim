div.pusher
  div.main.ui.container.top
    h1.ui.header= t('projets.new')
    p Marche à suivre : munissez vous de votre avis d'impôt le plus récent. Cela nous permettra de pré-remplir les informations vous concernant, que vous pourrez modifier par la suite.

    = form_for @projet_courant, html: { class:'ui form' } do |f|
      .field
        = f.label :numero_fiscal
        = f.text_field :numero_fiscal

      .field
        = f.label :reference_avis
        = f.text_field :reference_avis

      .field
        = f.label :description
        = f.text_area :description

      = f.submit "Démarrez votre projet", class:'ui button'
